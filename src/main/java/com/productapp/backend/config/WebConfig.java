package com.productapp.backend.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebConfig is intentionally empty.
 * Local file serving (/uploads/**) has been removed — images are now
 * stored in AWS S3 and served via direct S3 public URLs.
 */
@Configuration
public class WebConfig {
}